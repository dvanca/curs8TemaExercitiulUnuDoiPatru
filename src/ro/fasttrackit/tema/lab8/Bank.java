package ro.fasttrackit.tema.lab8;

public interface Bank {
    String printSoldCont();
    int withdrawal(int amount);
    void deposit(int amount);
}






//2) A person needs a banking account. There are several banks that provide
// this service: ING, BT, BRD. The bank needs to provide the possibility to withdraw and deposit money.